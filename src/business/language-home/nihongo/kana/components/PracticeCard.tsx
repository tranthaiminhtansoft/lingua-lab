import { useEffect } from 'react';
import type { ReactNode } from 'react'; import type { KanaEntry } from '../types/kana';
export function PracticeCard({ entry, onNext, speechControl }: { entry: KanaEntry; onNext: () => void; speechControl: ReactNode }) { useEffect(() => { const listener = (event: KeyboardEvent) => { const target = event.target; const active = document.activeElement; const editable = [target, active].some((item) => item instanceof HTMLElement && item.matches('input, textarea, select, [contenteditable="true"]')); if (event.key === 'ArrowRight' && !event.altKey && !event.ctrlKey && !event.metaKey && !event.shiftKey && !editable) onNext(); }; window.addEventListener('keydown', listener); return () => window.removeEventListener('keydown', listener); }, [onNext]); return <section className="panel practice" aria-labelledby="practice-title"><p>Practice card / <span lang="ja">練習カード</span></p><h2 id="practice-title">Recognize the sound</h2><div className="glyph" lang="ja" aria-live="polite">{entry.hiragana}</div><p className="cue">Hãy phát âm to chữ này</p>{speechControl}<button type="button" onClick={onNext}>Next</button></section>; }
